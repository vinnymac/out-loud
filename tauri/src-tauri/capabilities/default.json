{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main window: dialogs, opening external links, OS info, and core APIs.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default", "opener:default", "os:default"]
}
